{
  "name": "tradingview-claude-bot",
  "version": "1.0.0",
  "description": "TradingView webhook → Claude analysis → Discord alerts",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "test": "node test/send-test-alert.js"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "vercel": "^33.0.0"
  }
}
